node_modules
.env
.expo

# Expo managed workflow: native projects are generated by `expo prebuild`
# (locally or on the EAS build server) from app.json. They must NOT be
# committed or uploaded — a stale committed Info.plist silently overrides
# app.json and ships outdated permission strings. See SPEC-18 / SPEC-20.
/ios
/android

# Misc
.DS_Store
*.log
